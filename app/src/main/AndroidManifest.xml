<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moatasemabdallatif.miwok_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Numbers_Activity" android:label="@string/category_numbers"
            android:parentActivityName=".MainActivity"></activity>

        <activity android:name=".Family_Activity"
            android:label="@string/category_family"
            android:parentActivityName=".MainActivity"></activity>

        <activity android:name=".Colors_Activity"
            android:label="@string/category_colors"
            android:parentActivityName=".MainActivity"></activity>

        <activity android:name=".Phrases_Activity"
            android:label="@string/category_phrases"
            android:parentActivityName=".MainActivity"></activity>
    </application>

</manifest>